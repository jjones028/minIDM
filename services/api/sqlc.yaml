version: "2"
sql:
  - schema: "db/migrations"
    queries: "db/queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db/sqlc"
        sql_package: "pgx/v5" # Modern, high-perf Postgres driver
        emit_json_tags: true  # Useful for our eventual React API